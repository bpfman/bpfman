[maxbodylength]
# The help target is legitimately long (self-documenting list of
# every target). Raise the limit rather than splintering help across
# many small targets. Every other recipe in the Makefile is well
# under this ceiling.
maxBodyLength = 200
