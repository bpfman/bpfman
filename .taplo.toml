# taplo wants to reorder commands in alphabetical order, but order is important here
# vendor/ is regenerated wholesale by go mod vendor and its
# third-party tomls follow their own conventions.
exclude = ["Cross.toml", "vendor/**"]

[[rule]]

[rule.formatting]
indent_string = "    "
reorder_arrays = true
reorder_keys = true
