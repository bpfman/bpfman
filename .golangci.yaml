version: "2"
run:
  allow-parallel-runners: true
linters:
  enable:
    - loggercheck
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
formatters:
  enable:
    - gofmt
  exclusions:
    generated: lax
